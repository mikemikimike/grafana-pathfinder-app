# Review concern details

Detailed reviewer guidance, contract anchors, and named invariants for the compact routing registry in `docs/design/CONCERNS.md`.

Do not load this file wholesale during a review. Extract only activated concerns with `.cursor/skills/review/scripts/concern-context.mjs <concern-id>`.

## Concern review table

<!-- prettier-ignore -->
| id | purpose | load_docs | load_code | review_questions | one_way_doors | verification | related |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `security` | Protect the plugin from XSS, unsafe URL handling, insecure DOM APIs, unsafe HTML rendering, and other trust-boundary mistakes. | `.cursor/rules/frontend-security.mdc`, `.cursor/rules/systemPatterns.mdc` | `src/security/**`, `src/docs-retrieval/**`, `src/context-engine/**`, `src/interactive-engine/**`, `src/components/docs-panel/**`, `src/global-state/**`, `src/validation/**` | Are untrusted strings rendered safely?; Are URLs constructed and validated safely?; Are external payloads sanitized before use?; Does the change widen any trust boundary without explicit validation? | Persisting unsafe HTML or URLs; Emitting new sensitive data to external services; New writes to stateful external endpoints without validation | existing security tests; targeted tests for sanitization or URL validation changes | `docs-retrieval-and-rendering`, `context-engine`, `analytics-and-telemetry` |
| `correctness-and-reliability` | Catch correctness bugs, React anti-patterns, lifecycle leaks, missing cleanup, and avoidable regressions that can break the plugin even when subsystem contracts look fine in isolation. | `docs/design/PR_REVIEW.md`, `.cursor/rules/react-antipatterns.mdc` | changed frontend files only | Does the change introduce lifecycle leaks, stale closures, or cleanup bugs?; Does it introduce avoidable waterfalls or heavy sync work?; Is new code in the right layer or hook?; Are types, tests, and reuse opportunities reasonable?; Are new comments load-bearing (counterintuitive code, hidden invariants, external-bug workarounds) or decorative / restating the code (QC8)?; Are stale comments on touched code being left in place? | Semantic behavior changes without compatibility handling; State model changes that older code cannot interpret correctly | unit or integration tests for changed behavior; typecheck and lint | `testing-and-verification`, `cross-cutting-architecture` |
| `testing-and-verification` | Ensure meaningful behavior changes are verified with the right level of tests, especially for contracts, state machines, and risky edge cases. | `.cursor/rules/testingStrategy.mdc`, `docs/developer/E2E_TESTING_CONTRACT.md` | changed files plus nearby test files | Is there sufficient verification for the behavior being changed?; Are tests at the right level: unit, integration, contract, or E2E?; Are important failure modes and rollback scenarios covered? | Contract or schema changes without compatibility tests; External payload changes without assertions on old and new behavior | focused test coverage for new behavior; contract tests when public DOM/schema contracts change | `guide-schema-and-contracts`, `reversibility-and-one-way-door` |
| `reversibility-and-one-way-door` | Determine whether a change is safely reversible by reverting the PR, or leaves persistent state, semantic drift, or external side effects a revert would not undo. Includes browser-persisted state: tabs, progress, streaks, and completion data. | `docs/design/CONCERNS.md`, `.cursor/rules/systemPatterns.mdc`, `docs/developer/FEATURE_FLAGS.md`, `docs/developer/E2E_TESTING_CONTRACT.md`, `docs/developer/learning-paths/README.md` | changed files that read or write persisted state; changed files that send or shape external payloads | Does this PR change persisted data shape or interpretation?; Does it change the meaning of existing stored values?; Does it emit new or differently-shaped telemetry?; Does it change public DOM/test or schema contracts?; Would revert restore correctness, or only prevent further drift?; Does rollback require cleanup, migration, or replay?; Does reset/restore behavior still work?; Are progress and badge events still coherent?² | Storage format changes; Completion or progress semantics changes; Analytics payload schema changes; Recommender request shape changes; Public contract changes (`data-test-*`, schema values, API payload semantics); New writes to external stateful endpoints | backward-compatibility tests; explicit migration or fallback handling; rollback notes for non-reversible changes | `analytics-and-telemetry`, `guide-schema-and-contracts`, `context-engine` |
| `cross-cutting-architecture` | Catch interaction risks across subsystems, architecture drift, and changes that look locally correct but break established boundaries or system-wide expectations. Includes Grafana plugin conventions, Scenes, and repo-specific UI conventions. | `.cursor/rules/systemPatterns.mdc`, `.cursor/skills/design-review/ARCHITECTURE_PRINCIPLES.md`, `AGENTS.md`, `CLAUDE.md` | changed files only | Does the change respect engine boundaries?; Does it create an implicit contract that should be explicit?; Does it degrade gracefully when dependencies fail?; Does it align with Grafana conventions and plugin architecture?; Is sidebar/auto-open behavior preserved?; Is the approved dnd library used?; Does new UI text follow sentence case? | New hidden coupling across engines; Public behavior changes that other subsystems implicitly depend on; Route or lifecycle semantics changes; User-facing behavior changes persisted across sessions | cross-subsystem reasoning in the final synthesizer | all¹ |
| `context-engine` | Protect the recommendation pipeline's privacy, fallback, timeout, and semantic-tagging invariants. | `docs/developer/engines/context-engine.md`, `.cursor/rules/systemPatterns.mdc` | `src/context-engine/**`, related changed call sites | Are privacy guarantees preserved?; Are fallback tiers still correct and graceful?; Is debouncing still hook-level rather than fragmented?; Are recommendation semantics and filtering stable?; Are learning path and interactive completion stores still handled correctly? | Request payload shape changes; Identifier hashing changes; Completion semantics changes; Recommendation type or accuracy interpretation changes | context engine tests; privacy and fallback tests | `security`, `analytics-and-telemetry`, `reversibility-and-one-way-door` |
| `docs-retrieval-and-rendering` | Protect the fetch-parse-render pipeline, especially where remote or bundled content becomes rendered UI and interactive elements. | `.cursor/rules/systemPatterns.mdc`, `.cursor/rules/frontend-security.mdc`, `docs/developer/E2E_TESTING_CONTRACT.md` | `src/docs-retrieval/**`, changed docs panel consumers | Is content fetched and transformed safely?; Are fallbacks preserved?; Are interactive affordances still wired correctly?; Could this break rendering for bundled or remote content unexpectedly?; Are the docs-panel bootstrap signals (`__pathfinderPluginConfig`, `pathfinder-sidebar-mounted`, `docs-panel-container`, `pathfinder-auto-open-docs`) preserved, or updated atomically with the guide runner and contract tests? | Content parsing semantics changes; Rendered output or URL rewriting changes; Downstream assumptions about content shape changing silently | docs retrieval tests; rendering tests; docs-panel bootstrap contract tests; security tests if HTML handling changes | `security`, `interactive-engine`, `guide-schema-and-contracts` |
| `interactive-engine` | Ensure interactive automation remains safe, cancellable, and operationally coherent, including the DOM selector engine and recovery subsystem that automation depends on. | `docs/developer/engines/interactive-engine.md`, `docs/developer/E2E_TESTING_CONTRACT.md` | `src/interactive-engine/**`, `src/recovery/**`, `src/lib/dom/**`, changed interactive consumers | Are automated actions still cancellable?; Are observers, timers, and overlays cleaned up?; Are external URLs still validated safely?; Is auto-completion still opt-in and isolated from automation?; Are blocking and z-index rules still safe? | Changed action semantics; Changed completion event meanings; Changed navigation or execution behavior that affects guide assumptions | interactive engine tests; guided or multistep tests; contract tests if public behavior changes | `requirements-manager`, `guide-schema-and-contracts`, `security` |
| `requirements-manager` | Protect the gating and auto-completion semantics that interactive steps depend on. | `docs/developer/engines/requirements-manager.md`, `.cursor/rules/interactiveRequirements.mdc` | `src/requirements-manager/**`, `src/global-state/guide-identity.ts` | Is the phase order still objectives, eligibility, requirements?; Do unknown requirement types still fail open with warning?; Are retries, reactive checks, and mounted-state guards still correct?; Could this block guides or auto-complete them incorrectly?; Do guide-scoped checks (`var-*`, `section-completed:`) still resolve against the rendering guide's identity via `useGuideRequirements`, never a global or a shared default? | Requirement string semantic changes; Completion reason changes; Fix-type contract changes | requirements tests; guide flow tests | `interactive-engine`, `guide-schema-and-contracts` |
| `guide-schema-and-contracts` | Protect JSON guide schemas, package schemas, E2E DOM contracts (`data-test-*`), and TypeScript contract alignment. | `.cursor/rules/schema-coupling.mdc`, `docs/developer/package-authoring.md`, `docs/developer/E2E_TESTING_CONTRACT.md` | changed schema or validation files; changed bundled guides; `src/components/interactive-tutorial/**`; `src/interactive-engine/e2e-attributes.ts` | Are types and schemas aligned?; Are new fields explicit and validated?; Does older content still work?; Is there a breaking `data-test-*` or schema value contract change?; Are contract tests updated?; Is validation strict/tolerant in the right places? | Schema value renames; Removed fields; Validation tightening stranding content; `data-test-*` attribute renames/value/presence changes; Contract semantics changes without versioning | schema validation tests; fixture updates; contract tests; E2E updates if needed | `testing-and-verification`, `reversibility-and-one-way-door`, `interactive-engine`, `requirements-manager` |
| `build-and-ci` | Protect the build pipeline, CI behavior, release automation, and workflow safety so repository operations remain reproducible, secure, and understandable. | `AGENTS.md`, `CLAUDE.md`, `docs/developer/RELEASE_PROCESS.md` | changed workflow and build configuration files only | Does this preserve intended CI coverage and failure behavior?; Does it widen permissions or secret exposure unnecessarily?; Does it change release, publish, or artifact semantics?; Could a wording or logging change mask an important workflow state or error?; Are branch protections, matrix changes, caches, and environment setup still coherent? | Release or publish behavior changes; Artifact naming or retention changes; Permission changes that can leak credentials or mutate external state; Workflow steps that publish, deploy, sign, or write to external systems | reasoned review of changed workflow semantics; targeted command validation if a build script or release command changed | `security`, `testing-and-verification`, `reversibility-and-one-way-door` |
| `cli-and-e2e-runner` | Protect command-line semantics, manifest-aware preflight behavior, structured exit outcomes, and E2E runner correctness so developer tooling remains predictable, scriptable, and operationally efficient. | `docs/developer/E2E_TESTING.md`, `docs/developer/E2E_TESTING_CONTRACT.md`, `.cursor/rules/testingStrategy.mdc` | changed CLI command and utility files; directly related CLI tests | Do CLI flags fail fast for bad input?; Are skip, fail, and pass outcomes clearly distinguished and machine-usable?; Are expensive checks ordered so unrunnable guides short-circuit early?; Are exit codes and error messages consistent with the contract?; Do preflight checks avoid unnecessary network or browser startup work?; Are tests covering changed command semantics?; Does every new import keep the Node-context closure browser-free (environment-reachability test in `src/validation/architecture.test.ts`)? | Changed exit-code semantics; Changed skip-vs-fail behavior; Structured output changes; New preflight network calls before an expected early exit | CLI unit tests for changed semantics; behavior tests for skip, fail, and success outcomes; targeted smoke coverage when flag parsing or orchestration changes | `testing-and-verification`, `guide-schema-and-contracts`, `reversibility-and-one-way-door`, `build-and-ci` |
| `ai-subsystem` | Protect the repository's agent-facing behavior: prompts, routing rules, skills, long-lived agent instructions, and other files that shape how AI agents understand, review, edit, or execute work in this codebase. | `AGENTS.md`, `CLAUDE.md`, `docs/design/CONCERNS.md` | changed agent-facing docs and rules only; directly related skill, rule, or prompt files | Does this change alter what agents are allowed or encouraged to do?; Are routing rules, priorities, or safety constraints still coherent?; Could the new instructions cause agents to over-act, under-act, or contradict other guidance?; Are prompts specific enough for faster models to behave well?; Does the change preserve the intended contract between general docs and executable review logic?; Is this area important enough that CONCERNS.md should model it further? | Prompt or rule changes that steer agents toward unsafe or low-signal behavior; Changes that create contradictions between long-lived agent instructions; Routing or review logic changes that silently reduce review coverage; Agent-facing format changes that other prompts or workflows depend on | dry-run reasoning about likely agent behavior; consistency check against neighboring rules and skills; explicit review of contradictions, missing constraints, and degraded coverage | `cross-cutting-architecture`, `reversibility-and-one-way-door`, `build-and-ci` |
| `go-backend` | Catch Go-specific correctness, resource safety, concurrency, and operational risks in the plugin backend. | `docs/design/PR_REVIEW.md`, `AGENTS.md` | changed Go files only | Are errors handled?; Are resources closed and goroutines cancellable?; Is shared state safe?; Is input handling and credential handling safe? | Persisted backend state changes; API or stream semantic changes; External side effects that a revert would not clean up | `npm run lint:go`; `npm run test:go`; `go build ./...` | `reversibility-and-one-way-door`, `security` |
| `coda-terminal` | Protect the terminal UI's contract with the separate grafana-coda-app plugin: session handshake correctness, resource lifecycle, graceful degradation when the plugin is absent, and failure handling. | `docs/developer/CODA.md`, `.cursor/rules/coda.mdc`, `.cursor/rules/systemPatterns.mdc` | `src/integrations/coda/**`, `src/requirements-manager/checks/coda.ts`, `src/components/interactive-tutorial/terminal-connect-step.tsx`, `src/components/interactive-tutorial/challenge-block.tsx`, `src/components/block-editor/forms/TerminalConnectBlockForm.tsx`, `src/components/AppConfig/CodaBackendStatus.tsx` | Are trust boundaries preserved (no credentials or VM IPs in frontend code, exec always session-scoped)?; Is resource cleanup correct for subscriptions, timers, intervals, SSH sessions, and goroutines on unmount, disconnect, and error?; Is the Live channel treated as opaque (using the backend's returned `channel`, never hand-constructed)?; Does the code degrade quietly when `grafana-coda-app` is absent or unregistered?; Are `sessionStorage`/`localStorage` key names and shapes backward-compatible?; Are failure modes handled (VM provisioning timeout, SSH auth retries, quota cleanup, handshake timeout, Live channel disconnect)?; Does the `is-terminal-active` requirement stay in sync with `getTerminalConnectionStatus()`? | The v1 session/exec API shape (owned by grafana-coda-app); `coda.session.v1` frame shape and `SessionEvent` type values; Storage key names and shapes in `terminal-storage.ts`; `JsonTerminalConnectBlock` and `JsonChallengeBlock` schema fields | `npm run test:ci`; `src/requirements-manager/terminal-requirement.test.ts`; `src/requirements-manager/checks/coda.test.ts`; `src/components/interactive-tutorial/challenge-block.test.tsx`; `src/integrations/coda/*.test.ts` | `go-backend`, `guide-schema-and-contracts`, `interactive-engine`, `requirements-manager`, `security`, `reversibility-and-one-way-door` |
| `mcp-authoring-server` | Protect the MCP authoring server's trust boundaries, tool contract stability, transport semantics, artifact integrity, and CDN integration so the AI-driven authoring surface remains safe and predictable. | `docs/developer/MCP_SERVER.md`, `docs/design/HOSTED-AUTHORING-MCP.md`, `docs/design/AUTHORING-SESSION-ARTIFACTS.md` | `src/cli/mcp/**` | Does this change alter the HTTP listen address or allowed origins?; Is artifact ETAG integrity still enforced?; Are tool names, input schemas, or outcome types stable?; Could this expose sensitive data through access logs or error responses?; Are request size caps, timeouts, and concurrency limits still coherent?; Does a CDN fetch change affect fallback behavior? | HTTP transport binding changes; ETAG algorithm or computation changes; tool name or input schema changes; CDN URL or fetch semantics changes; access log field additions that capture sensitive data | `npm run test:ci`; targeted tests for new tool contracts or transport changes | `cli-and-e2e-runner`, `guide-schema-and-contracts`, `security` |
| `package-engine` | Protect the package resolution fallback chain (bundled → recommender → CDN), manifest validation, and content loading contracts so content discovery degrades gracefully and never silently serves stale or malformed packages. | `docs/developer/package-authoring.md`, `docs/design/PATHFINDER-PACKAGE-DESIGN.md` | `src/package-engine/**`, `src/bundled-interactives/repository.json` | Is the resolution tier order still correct?; Could a package ID mismatch silently skip all tiers?; Is schema validation enforced at load time?; Would a malformed manifest break downstream consumers?; Does a bundled repository change strand any existing package IDs? | Package ID registry changes (repository.json); resolution tier order changes; removal of load-time validation; manifest schema field renames or removals | `npm run test:ci`; `npm run validate:packages` | `guide-schema-and-contracts`, `docs-retrieval-and-rendering`, `reversibility-and-one-way-door` |
| `assistant-integration` | Protect the inline assistant integration's prompt construction, metadata inclusion, tool contract stability, and trust boundary with the external LLM so the assistant cannot be fed sensitive context or made to suggest unsafe actions. | `docs/developer/ASSISTANT_INTEGRATION.md` | `src/integrations/assistant-integration/**` | Does this change alter what metadata is included in prompts (datasource lists, Grafana version, auth info)?; Could it expose sensitive user context to the LLM?; Are tool definitions still correct and minimal?; Is assistant availability gating still coherent?; Could this cause the assistant to suggest or execute unsafe actions? | Tool definition changes (name, schema, behavior); context builder changes that alter what Grafana metadata is exposed; changes to when the assistant is enabled or disabled | `npm run test:ci`; review of prompt construction logic for any new metadata fields | `security`, `analytics-and-telemetry`, `docs-retrieval-and-rendering` |
| `workshop-collaboration` | Protect the workshop session's cryptographic trust model, P2P connection lifecycle, action replay semantics, and attendee state coherence so collaborative sessions cannot be hijacked or desynchronized. | `docs/developer/LIVE_SESSIONS.md` | `src/integrations/workshop/**` | Is the ECDSA key lifecycle still correct (generation, storage, cleanup on disconnect)?; Are nonce-based challenge/verify semantics preserved?; Could action replay be triggered by an unauthenticated peer?; Is attendee state cleaned up on disconnect?; Are P2P connection errors handled safely without leaking session state? | Signature algorithm or nonce semantics changes; session state shape changes that break reconnection; action event type changes that affect replay fidelity; private key storage or lifecycle changes | `npm run test:ci`; manual session testing for reconnection and replay scenarios | `security`, `reversibility-and-one-way-door`, `interactive-engine` |
| `cross-tab-controller` | Protect the two-tab controller's trust boundary: the `pathfinder-cross-tab` `BroadcastChannel` is same-origin and forgeable, so every side-effecting command (`step-command`, `check-requirements`, `fix-requirement`, `sidebar-handoff`) must pass per-kind shape validation and carry a valid ECDSA signature from the paired controller before dispatch, pairing must require a trusted user gesture, and the live-tab executor must never drive navigation or DOM mutation on behalf of an unpaired or unsigned sender. | `docs/developer/CROSS_TAB_CONTROLLER.md` | `src/integrations/cross-tab/**`, `src/lib/cross-tab-transport.ts`, `src/lib/pairing-manager.ts`, `src/security/cross-tab-crypto.ts`, `src/types/cross-tab.types.ts`, `src/global-state/controller-channel.tsx` | Does this change add a new message kind without a case in `KIND_VALIDATORS`?; Does it widen `KNOWN_TARGET_ACTIONS` without security review?; Does it weaken field validation in any per-kind validator, especially `check-requirements` or `fix-requirement`?; Does it remove or bypass the double-validation gate (transport receive AND executor sink)?; Does it weaken the executor auth gate (`verifySignedMessage`) — the accepted-session check, the `sessionId`/`liveTabId` binding, the signed-message timestamp window, or the replay-nonce ledger?; Does it alter the HMAC launch binding or pairing challenge (`pairing-manager.ts`, `createControllerPairingLaunch`)?; Does it make the per-session ECDSA key extractable or change its lifecycle (`cross-tab-crypto.ts`)?; Does it let a session be accepted without a trusted user gesture (`acceptSession(..., trustedGesture)`)?; Does it alter when the executor installs relative to when the `PairingRequestBanner` listener is live?; Does it add navigation, form-fill, or DOM-mutation reachability via a new field or message kind?; Does it change the `shouldMountSidebar` / `pathfinderEnabled` entry guard? | New message kind without a validator; Widening `KNOWN_TARGET_ACTIONS` (each new verb is a new DOM-mutation surface); Removing the double-validation gate; Weakening or bypassing the command signature/auth gate (`verifySignedMessage`); Making the per-session signing key extractable; Allowing executor install before the pairing banner is live; enabling the `enableTwoTabController` admin setting or changing its default-off (makes the executor live for an opted-in instance — any unfixed trust-model gap becomes exploitable by real users and co-plugins) | `npm run test:ci`; `validateCrossTabMessage` and per-kind validator contract tests; `src/integrations/cross-tab/cross-tab-protocol.acceptance.test.tsx`; `src/integrations/cross-tab/live-tab-executor.test.ts`; `src/lib/pairing-manager.test.ts` | `security`, `interactive-engine`, `requirements-manager`, `reversibility-and-one-way-door` |
| `block-editor-authoring` | Keep block-editor authoring state coherent across Edit, Preview, and JSON transitions; panel remounts and browser recovery; guide replacement; recording; and backend draft/publish handoff. Guide content, block IDs, JSON drafts, recording state, and backend tracking must not be restored or discarded independently. | `docs/developer/components/block-editor/README.md`, `docs/developer/CUSTOM_GUIDES.md`, `.cursor/rules/react-antipatterns.mdc` | `src/components/block-editor/**` | Does one mount-time path own restoration of the guide, block IDs, view mode, and JSON draft?; Do Edit, Preview, and JSON transitions preserve or apply the correct draft atomically?; Do New, Import, Template, and Library replacement clear unrelated JSON, recording, and backend-tracking state before persistence observes the replacement?; Does every discard promise match what remains in localStorage?; Are stored shapes defensively validated and backward-compatible?; Do component tests exercise a full unmount/remount rather than only isolated hooks?; Do draft, publish, unpublish, and library-load flows keep backend tracking aligned with the canonical guide? | `BLOCK_EDITOR_STORAGE_KEY` shape or version changes; `ViewMode` or `JsonModeState` semantics; block ID persistence used by recordings; guide replacement or discard behavior; backend `resourceName`, status, or `lastPublishedJson` tracking changes | `npm run test:ci`; `BlockEditor.persistence.test.tsx`; `useBlockPersistence.test.ts`; `useBackendSaveFlow.test.ts`; `useJsonModeHandlers.test.ts`; `useGuideOperations` and backend-guide tests when replacement or publishing changes | `reversibility-and-one-way-door`, `testing-and-verification`, `floating-panel` |
| `data-check` | Keep the plugin's only data source query path bounded, user-triggered, and honest about what it verified: a check runs on a click and never on a timer or a requirements token, only against a data source the picker currently offers, a query that could not run is never reported as absent data, and a failure holds a section up only where the author asked it to. | `docs/developer/interactive-examples/json-guide-format.md` | `src/lib/datasource/**`, `src/components/interactive-tutorial/datasource-check-step.tsx`, `src/components/interactive-tutorial/use-data-check.ts` | Does this add a second caller of `/api/ds/query`, or move the cost caps out of `runDataCheckQuery`?; Could a check now run without a click — from an effect, a timer, or a requirements token?; Does an authored `defaultValue` now let a check run against a data source the user never picked?; Can a check run against a data source the picker does not currently offer?; Does a failing check block a section the author did not mark `dataCheckBlocking`?; Does 'has data' still mean at least one row rather than at least one frame?; Can a timeout or transport error still surface as the author's no-data message?; Does giving up on a check abort the query it started?; Does any telemetry or log now carry the query text or the data source name? | New `/api/ds/query` callers; changes to `DATA_CHECK_QUERY_LIMITS`; running a check from an effect rather than a handler; removing the offered-options validation of a restored pick; making `input` tracked for any shape other than a blocking check | `npm run test:ci`; the 11-point browser checklist in the PR that established this concern | `interactive-engine`, `requirements-manager`, `guide-schema-and-contracts`, `analytics-and-telemetry`, `reversibility-and-one-way-door` |
| `floating-panel` | Protect the floating panel's dodge/compact/restore lifecycle: the `pathfinder-floating-*` event ordering contract (a compact precedes its dodge move; a position restore precedes the full restore when obstacles clear), the dodge-session invariant that layout-derived values like `scrollTop` are not authoritative while a session holds saved state, token staling of deferred scroll writes, and geometry/mode persistence across drag, resize, minimize, and pill restore. | `.cursor/rules/systemPatterns.mdc`, `.cursor/rules/react-antipatterns.mdc` | `src/components/floating-panel/**`, `src/global-state/panel-mode.ts`, `src/lib/event-names.ts` | Is the `pathfinder-floating-*` dispatch order preserved?; Does every session transition that can stale a pending deferred scroll write bump the restore token?; Is a layout-derived `scrollTop` ever adopted while the session already holds a saved scroll?; Are document listeners, observers, and timers cleaned up on unmount?; Does manual drag still retarget the pre-dodge restore position instead of teleporting the panel?; Do minimize and pill restore preserve scroll and geometry? | `pathfinder-floating-*` event name or payload shape changes (tests deliberately pin the raw literals); `data-panel-state` value changes (test contract); persisted geometry shape changes (`panelModeManager` storage) | `npm run test:ci`; `dodge-session.test.ts`; `FloatingPanel.dodge-session.test.tsx`; `useHighlightDodge.test.ts` | `interactive-engine`, `performance-and-bundle`, `cross-cutting-architecture` |
| `completion-records` | Keep durable completion honest end to end: one emission boundary per terminal completion, one identity — the verified ID-token `sub`, never the outbound on-behalf-of credential — one record name (`hash(userID ‖ idempotencyKey)`) so a retried POST dedupes rather than minting a second record, one bounded queue that retains records an environment cannot accept yet instead of dropping them, and one denominator (`src/lib/guide-stats`) that publishes the block count and the per-block positions together so a numerator and a denominator can never come from two traversals. | `docs/design/BACKEND_PROXY_PATTERN.md`, `.cursor/rules/systemPatterns.mdc (tier-1 lib/ guide-stats bullet)`, `docs/developer/STEP_MODEL.md` | `src/completion-records/**`, `src/lib/guide-stats/**`, `pkg/plugin/completion_records_write.go` | Is completion still keyed on `(repository, manifest.id)` read off the resolved manifest and never off a loader URL, with a failed resolution supplying no repository?; Do the stamped `spec.userId` and the read join key still derive from the verified ID-token `sub` rather than from the outbound on-behalf-of token?; Does every POST still carry the event's stable id as a required idempotency key, so a re-send after a lease takeover dedupes to one durable record?; Does the response-status retry taxonomy still hold — 401/408/429/5xx/3xx transient, 403 and 404 disarm-and-retain, other 4xx terminal — and does a disarm still keep the queued records for a later drain?; Is write health still reported through the typed outcome taxonomy rather than a latency measurement?; Does normalization still reject a fact whose identifiers stay invalid at emission, instead of letting it occupy the bounded queue until a terminal 400?; Does a drain still halt on a failed lease renewal rather than re-establishing one, and is the in-flight item still pinned against an at-capacity `enqueue`?; Does a journey still complete on whole-set membership of its current milestones rather than on a count?; Does this change compute a stamped manifest denominator, or the percentage recorded on a durable completion record, anywhere outside `src/lib/guide-stats`? (The per-step model in `completion-store.ts` and the path rollup in the learning-paths hook are deliberately out of scope — see `docs/developer/STEP_MODEL.md`.); Does a consumer of the stamped denominator index the pre-inlining tree? (Mapping an inlined block back to its `snippet-ref` is not available — the splice carries no provenance.); Does a change to the counting rule bump `GUIDE_STATS_VERSION` and account for manifests already stamped under the old rule?; Does a block type newly treated as completable actually emit completion evidence at runtime, per `completion-affordance.parity.test.ts`? | Record-name derivation changes (`hash(userID ‖ idempotencyKey)`) — rekeys every future record and stops it deduping against already-durable ones; retry-taxonomy reclassification (moving a status between transient, terminal, and disarm-and-retain); the user/org-scoped `localStorage` queue prefix or persisted queue shape (an unreadable queue is silently orphaned progress); the 30-day retention horizon; block-counting rule changes (restate the denominator of every already-stamped manifest and every percentage already recorded against it) | `npm run test:ci`; `npm run lint:go`; `npm run test:go`; `src/completion-records/completion-write-queue.test.ts`; `src/lib/guide-stats/*.test.ts`; `pkg/plugin/completion_records_write_test.go`; `pathfinder-cli build-stats <root> --check` | `security`, `go-backend`, `cli-and-e2e-runner`, `guide-schema-and-contracts`, `analytics-and-telemetry`, `reversibility-and-one-way-door` |
| `full-screen-sidebar-handoff` | Protect the full-screen ↔ sidebar surface handoff: when it triggers (click-driven, not proactive on load), that every action-execution path a step can run through (`executeInteractiveAction`, guided steps, code-block Insert) applies the same gate, and that any author-controlled navigation target it resolves is validated before reaching `locationService.push`. | `.cursor/rules/systemPatterns.mdc` | `src/components/full-screen/**`, `src/global-state/panel-mode.ts`, `src/interactive-engine/interactive.hook.ts` (handoff gate call site), `src/components/interactive-tutorial/interactive-guided.tsx`, `src/components/interactive-tutorial/code-block-step.tsx` | Does every action-execution path that can run inside full screen (standalone step, multi-step, section sequence, guided step, code-block Insert) apply the handoff gate, not just `executeInteractiveAction`'s direct callers?; Is a resolved `targetPath` validated (`resolveSafeTargetPath` / `validateRedirectPath`) before `locationService.push`, the same way `NavigateHandler` validates its own author-controlled target?; Does the sidebar mount-wait have a bounded safety timeout so a missed mount event can't hang the click?; Do existing `handleExitToSidebar` call sites (`manual_exit`, `empty_state_fallback`, `dock_request`) stay unaffected by new optional parameters? | New action-execution path added without the handoff gate; unvalidated navigation target reaching `locationService.push`; `REQUEST_SIDEBAR_HANDOFF_EVENT` payload shape changes; removing the mount-wait safety timeout | `npm run test:ci`; `sidebar-handoff-wiring.contract.test.ts`; `resolve-safe-target-path.test.ts`; `panel-mode.test.ts`'s `requestSidebarHandoffAndWait` suite; `interactive.hook.fullscreen-handoff-timing.test.ts` | `interactive-engine`, `floating-panel`, `security`, `docs-retrieval-and-rendering` |
| `analytics-and-telemetry` | Ensure analytics, observability, feature flags, and experiment rollout remain privacy-safe, semantically stable, safely defaulted, and operationally useful. | `docs/developer/FEATURE_FLAGS.md`, `docs/developer/TELEMETRY.md` | `src/lib/analytics.ts`, `src/lib/faro.ts`, `src/lib/telemetry/**`, changed call sites; changed feature flag definitions or call sites | Are event semantics changed?; Could it leak sensitive or high-cardinality data?; Are OSS and Cloud behaviors still correct?; Are experiment and flag enrichments still coherent?; Does this create irreversible downstream data pollution?; Are flag defaults safe?; Does behavior degrade gracefully when flag evaluation fails?; Does new feature behavior add a silent fallback or latency-sensitive path with no instrumentation (see `docs/developer/TELEMETRY.md`)? | Event name changes; Payload schema changes; New user-derived fields; Cloud/OSS telemetry routing changes; Default behavior changes altering cached/persisted state; Flag semantic changes without compatibility | analytics tests; privacy review for new fields; tests for enabled and disabled flag states; fallback behavior tests | `reversibility-and-one-way-door`, `security` |
| `performance-and-bundle` | Catch regressions in render cost, monitoring breadth, timeout usage, network waterfalls, and bundle growth. | `.cursor/rules/systemPatterns.mdc`, `.cursor/skills/design-review/ARCHITECTURE_PRINCIPLES.md` | changed files adding monitoring, async fetches, or heavy imports | Does this widen observation scope more than necessary?; Does it introduce avoidable waterfalls or heavy render work?; Does it bypass shared timeout or debounce patterns?; Does it grow the initial bundle unnecessarily? | New persistent performance cost from always-on listeners or observers; Bundle-loading changes that become hard to unwind after shipping dependencies | performance reasoning in review; targeted tests or profiling when risk is high | `context-engine`, `interactive-engine`, `cross-cutting-architecture` |

---

## Contract anchors

A contract anchor records the deliberately established contract of a capability: its owning module, its load-bearing invariants, and the PR or issue that established it. The conditional contract evolution scan in `.cursor/skills/review/SKILL.md` checks new PRs for **conformance** against a recorded anchor; when no anchor exists it must reconstruct the implied contract from recent PR history, which is slower and weaker.

Rules:

- Anchors are optional per concern — record one when a PR deliberately establishes or replaces a contract (a typed facade, reducer, schema, or lifecycle owner), in that same PR.
- An anchor states invariants and ownership, not implementation detail. If the invariant is already captured in the concern's `purpose`, the anchor may simply point at it.
- Anchors are evidence, not authority: a scan that finds the anchored contract incoherent should propose replacing it, not enforce it.
- Name contracts after the domain, never the vendor — a telemetry contract, not a "Faro contract".
- Add a concern routing row when a capability directory is created, not when its contract is finally written — the gate cannot see a concern's history until the row exists, and rows that arrive in the contract-establishing PR miss the fracture window they were meant to watch.

<!-- prettier-ignore -->
| concern | anchor | contract |
| --- | --- | --- |
| `ai-subsystem` | #1711 → #1721 | `docs/design/PR_REVIEW.md` owns the canonical observation, truth-verdict, contract-evolution, reconciliation, state, and report schemas. `.cursor/skills/review/SKILL.md` owns Route → Observe → Verify → Dispose → Reconcile → Publish orchestration. `concern-context.mjs` bounds first-round review to two general workers plus one gated contract specialist, incremental review to one general worker plus one gated specialist, and every worker packet to eight files and 30,000 characters; the root owns synthesis and overflow. `review-policy.mjs` is the only disposition and reconciliation authority: producers state facts, skeptics decide truth, protected harm and one-way doors are derived, authorship and round precedence are closed, and downstream phases cannot change a final result. `review-report.mjs` is a passive renderer and strict state parser. Complete v2 state carries full blocker, deferred, and clearance values under one size limit; overflow emits only empty lists plus `truncated: true` and forces a full next review. Version 1 remains read-compatible. Same-reviewer provenance, ancestor validation, recap adjacency, and fail-closed malformed state remain trust-boundary preconditions. Invariants in `purpose`. |
| `floating-panel` | #1298 → #1300 | The dodge-session reducer (`dodge-session.ts`) owns the compact/restore lifecycle; layout-derived values like `scrollTop` are not authoritative while a session holds saved state; `FloatingPanelEvents` owns the event names. Invariants in `purpose`. |
| `block-editor-authoring` | #1314 → #1682 | One mount-time path owns restoration; `useBackendSaveFlow` owns save/publish/unpublish orchestration; `useBlockPersistence` is narrowed to `clear`. The editor is a partial writer of a whole-object-replace resource: `saveGuide` layers the editor-owned `spec` fields over the spec last read, and all three writes carry `metadata.annotations` and `metadata.labels` through, so `spec.manifest` and upload-script provenance survive a save the editor did not author them in. Inheritance is scoped to the resource the editor's content came from, and backend tracking is what encodes that — so tracking is set only after a write lands, never before. Overwriting an unrelated resource on a name collision therefore keeps the `resourceVersion` guard but inherits nothing, and a failed overwrite stays untracked so the retry re-asks: inheriting there would render the new guide as the old one's path and stamp it with a provenance mark the upload script's ownership check trusts. The spec passthrough comes from the LIST snapshot, so the conflict check guards what is being replaced for as long as that snapshot stands; re-reading the resource at save time would defeat it outright. All three writes merge caller-supplied metadata over the snapshot's per field, so partial metadata cannot suppress the `resourceVersion` guard, and all three fail closed when the named resource is absent rather than replacing a spec they cannot see. Absence is confirmed against the server before refusing, by a probe that does not commit its result: a transient LIST failure resolves to an empty list rather than an error, so trusting the snapshot alone would make a guide permanently unsaveable — while committing the probe would clear a live error and store that empty list, hiding the guide-library entry that is the only way to recover. The editor is also a manifest PRODUCER, not only a preserver: `deriveManifest` stamps the fields the content determines — `type` when none was inherited, `repository` when minting a fresh manifest, and `additionalFields.stats` / `additionalFields.startingLocation` — and leaves every other manifest field exactly as read, so an editor-authored guide is a complete package while a path cover page keeps its `type` and `milestones`. Stats are stamped only for a plain `guide`, checked positively so an unrecognised type yields no stamp rather than a wrong one, and an inherited rollup is never replaced with cover-page counts. `startingLocation` is owned CONDITIONALLY, decided per save from provenance rather than from the value: the editor writes or clears it only when there is nothing inherited to lose, or when the inherited value is exactly what the derivation produces from the blocks read alongside that manifest. That is what lets an author who deletes the `on-page:` requirement clear the prompt with it, while an uploaded package's independently authored value survives a title-only edit instead of being erased — the two failure modes pull opposite ways and only provenance separates them. A value that diverges stays the external author's; reclaiming it belongs to an explicit manifest control, not a silent overwrite. Derivation traverses in the same order as `walkBlocks` in the cross-block lint, and "first executable block" means the same thing in both, so the manifest cannot disagree with the entry-location advice `firstStepMissingOnPage` gave the author — including accepting a first-step `formfill`'s declaration, which the lint accepts and the editor itself suggests. The stamp goes under `additionalFields` because the CRD's `#Manifest` declares eight keys and prunes the rest with a 201 and a `Warning:` header `getBackendSrv()` never surfaces — so the invariant that the derivation introduces no undeclared top-level key is a test, not a comment. Fracture: #1599, #1600. Invariants in `purpose`. |
| `data-check` | #1597 → #1612 | `runDataCheckQuery` (`src/lib/datasource/run-data-check-query.ts`) is the sole owner of query execution and its cost caps (15s abort, 100-point cap where the query type honours it, default 1h range). `useDataCheck` owns the check lifecycle and its `AbortController`. A check runs on user action only — including an engine-driven click, which the interactive engine can dispatch document-wide — never on a polling cadence, and only against a data source the picker currently offers. On an advisory check an authored `defaultValue` can seed that pick, so there "offered" is the guarantee rather than "the user chose it"; on a blocking one the schema rejects `defaultValue`, so a gating check only ever runs against a data source the user chose. A verdict and any completion it earned belong to that data source, and are retracted whenever the resolved pick changes, whichever writer changed it. A query that ran and found nothing (`'no-data'`) and one that could not run (`'error'`) are distinct states, and the author's failure message speaks only for the first. Abandoning a check aborts it, and an aborted check reports no outcome and completes nothing. `dataCheckQuery` alone enables the check; `dataCheckBlocking` alone decides whether a failure gates a section, and is the only shape under which an `input` block becomes a tracked step. Invariants in `purpose`. |
| `interactive-engine` | #1616 → #1757 | `INTERACTIVE_ACTION_TYPES` in `src/types/interactive.types.ts` is the canonical runtime vocabulary for interactive verbs; raw DOM values are decoded by `extractInteractiveDataFromElement` before entering `InteractiveElementData`, while component-owned requirement pseudo-actions such as `section` remain explicitly string-widened through `InteractiveRequirementsData`.<br>`InteractiveGuided` owns idempotent persistence and completion callbacks for the guided completion lifecycle.<br>`GuidedHandler` owns listeners, timeouts, connectivity intervals, overlays, click ordering, cleanup, and terminal arbitration.<br>A terminal winner blocks later completion work. Successful final-action persistence remains completed through later setup or cleanup faults.<br>Final click activation can cause persistence during capture, before the application handler. Without this activation, completion persists after the result. |
| `full-screen-sidebar-handoff` | #1670 | `requestSidebarHandoffAndWait` (`src/global-state/panel-mode.ts`) is the sole facade for triggering the full-screen → sidebar handoff; the trigger is click-driven (a "Do it" on a Grafana-driving action) via `interactive.hook.ts`'s `executeInteractiveAction` gate, `interactive-guided.tsx`'s `executeStep`, and `code-block-step.tsx`'s Insert path — never proactive on content load. `resolveSafeTargetPath` (`src/components/full-screen/resolve-safe-target-path.ts`) is the sole validator for a resolved `targetPath` before it reaches `locationService.push` in `FullScreenPanel.tsx`'s `handleExitToSidebar`; an invalid or denied path is treated the same as "no signal" (see `full-screen-fallback-location.ts`), never as a forced navigate-to-root. The mount-wait (`pathfinder-sidebar-mounted` + settle, or a safety timeout) bounds how long a click can block. Invariants in `purpose`. |
| `coda-terminal` | #688 → #1468 | `grafana-coda-app` owns session lifecycle, VM provisioning, SSH, the relay, quota and credentials. Pathfinder owns the terminal UI only and is a v1 consumer through the single adapter `src/integrations/coda/coda-api.ts`; the Live channel address is opaque to it. Availability is resolved at runtime, never declared as a plugin dependency — and resolving it costs nothing when the operator has not enabled the terminal, nor reports an absent plugin as a fault (boot data before the settings fetch core logs). |
| `cli-and-e2e-runner` | #1372 → #1624 | `executeAllSteps` owns per-step verdicts and the abort decision. `guide-runner.spec.ts` owns the guide report outcome. `playwright-runner.ts` owns process-level fallbacks when Playwright does not produce a report. |
| `completion-records` | #1411 → #1700 | The Go write proxy (`pkg/plugin/completion_records_write.go`) owns the durable-write contract; `docs/design/BACKEND_PROXY_PATTERN.md` §11 states it. `deriveCompletionUserID` is the single identity contract — the verified ID-token `sub` verbatim — and both the read join key and the write's stamped `spec.userId` derive from it, never from the outbound on-behalf-of credential. The record name is `hash(userID ‖ idempotencyKey)`, so a retried POST targets the same object (409 is idempotent success) and two users' identical keys cannot collide. The response status IS the client's retry taxonomy: 401/408/429/5xx/3xx are transient and retried; 403 and 404 disarm writes for the session while RETAINING queued records for a later drain; other 4xx are terminal and dropped. The 404 disarm has three triggers — an upstream 404, `obo-unavailable`, and `identity-unverifiable` (a stack with no app URL can never verify a token, so it must disarm rather than take the retried 401). A write stales the namespace read index rather than deleting it, so a failed post-write refresh still serves the warm index. Client conformance is #1434. |
| `go-backend` | #1604 → #1700 | `pkg/plugin/auth/id_token.go` owns inbound caller-identity verification; `pkg/plugin/app_platform_identity.go` owns the single verdict every proxy route reads. Two properties, both required: the ID token's ES256 signature against the live key set of whichever authority issued it — the stack's own `/api/signing-keys/keys` self-hosted, auth-api on Grafana Cloud, both full verification and neither a weaker check. The chain ORDER is a contract property: the stack is asked FIRST because it is the authority for its own callers, and asking an unauthenticated in-cluster address ahead of it would let whatever answers that name decide who the caller is; auth-api is reached only where the stack publishes no matching key, which is the Cloud shape (`{"keys":null}`) — and its `namespace` claim bound by RAW equality to the server-derived namespace from the plugin context, never a request header and never the token's own claim. The signature alone does not identify a stack: auth-api's key set is cell-wide, so the binding is what refuses a genuine sibling-stack token. Raw equality means a claim of `*` is refused, deliberately diverging from authlib's `types.NamespaceMatches`. `Verify` takes the expected namespace as a parameter and enforces it internally, so a new call site cannot drop the binding. Failures are one `identityStatus`, so no route classifies a failure its own way: rejected (no acceptable token) is the only time-recoverable verdict and the only one that may take a retryable status; `identity-unverifiable` (nothing this stack supplies makes verification possible) and `signing-keys-unreachable` (no source answered at all) are standing conditions that must disarm rather than retry. A source that answers with a key set lacking the `kid` HAS answered — the normal Grafana Cloud shape — so that is a rejection, not an outage. A new status must be named in both `//exhaustive:enforce` switches. Not established: binding a token to its presenter — a copied, unexpired token replayed on the stack it was minted for still verifies, an accepted residual. `docs/design/BACKEND_PROXY_PATTERN.md` §3 states the boundary. |
| `mcp-authoring-server` | #1639 | `registerAuthoringTools` (`src/cli/mcp/tools/index.ts`) owns the tool catalog. Multi-verb CLI capabilities group into one resource tool carrying a kebab-case CLI-named `operation` plus an opaque, help-derived `opts` bag; append-only child members (`add-step`, `add-choice`) live on the same tool as operations. The CLI runners are the sole validator of **guide content**; a tool may validate its own **argument shape** — `validateCommandArgs` preflights `opts` against the exact interface `pathfinder_help` publishes (both rendered from the command's Zod schema), and mcp-native tools own explicit top-level Zod schemas. Placement and reparenting (`before`/`after`/`position`, `move-block`, `orphanChildren`) are deliberately withheld, and the withholding is declared where the tool binds the command rather than on the command itself — the CLI still offers all of them, and narrowing the agent procedure is this surface's decision. Withheld or unknown parameters are rejected (`UNSUPPORTED_PARAMETER`), never silently dropped. `pathfinder_help` is the discovery surface, and it is addressable only for commands an MCP tool has bound — a CLI command no tool dispatches reports `UNKNOWN_COMMAND` rather than publishing flags no tool accepts. Within a bound command, new CLI capability reaches agents without an adapter change unless a binding names runner fields. |
| `guide-schema-and-contracts` | #1662 | The manifest's top level is deliberately open: `ManifestJsonObjectSchema` and `RepositoryEntrySchema` are loose, so an unknown top-level key is extension metadata that survives parsing and is forwarded verbatim into the package's repository entry, unvalidated — the consumer owns an extension key's shape, because nothing checks it. Known fields keep their existing validation and their explicit entry mapping, so fields that deliberately do not reach an entry (`id`, `schemaVersion`, `repository`, `language`) still do not. `build-repository` owns the forwarding: it refuses the entry fields it computes (`path` from the package directory, `title` from `content.json`) plus `__proto__`, from a set derived from the two schemas' shapes rather than hand-maintained, warns on each refusal, and reports the keys it forwards. Nested manifest objects (`author`, `targeting`, `testEnvironment`) and every guide and block schema stay strict — the openness is the manifest's top level only. Promoting a forwarded key to a named field is the migration path: the explicit mapping takes over and the key keeps appearing, now validated. Annotation ownership (`managed-by` / `source-package`, evidence #1504) is a separate unlanded hypothesis and is not contracted here. |

### Named invariants

A named invariant is one load-bearing rule inside an anchored contract, given a stable name so a code comment or test name can cite it as ``see `<name>` `` instead of restating it. They are read the same way as anchors — evidence, not authority — and belong to the concern in the second column. Name the rule, never the code that happens to hold it today.

<!-- prettier-ignore -->
| name | concern | invariant |
| --- | --- | --- |
| `payload-boundary-normalization` | `completion-records` | Identifier and title fields are clamped to the backend's byte bounds, with control characters stripped, at emission into the queue — not at send. A fact whose identifiers are still invalid after normalization is rejected before it is queued rather than occupying the bounded queue until a terminal 400. The backend stays authoritative; this only spares the queue and surfaces the drop. |
| `disarmed-shared-queue-bound` | `completion-records` | The queue owns every unsent completion write, keyed per `(user, org)` owner. Its 100-item cap is per-tab and eventually-global, not strictly global: each tab enforces it against its own in-memory snapshot under a shared owner prefix, so N disarmed tabs can transiently persist ~N×100 keys until a drain reconciles. `localStorage` has no atomic multi-key transaction, so this is documented, not faked. |
| `queue-eviction-concurrency` | `completion-records` | Draining holds a storage lease, renewed immediately before each send. A lease a tab persisted and that has since vanished means takeover-or-clear, so renewal fails and the drain halts rather than re-establishing one — that is what extends a progress reset's discard beyond the resetting tab, since `clear()` sweeps the owner prefix and with it the lease. The in-flight item is pinned so a concurrent at-capacity `enqueue` cannot evict it, and no outcome re-persists a record that left the queue mid-send. Each queued event's stable id is the required idempotency key, so a re-POST after a lease takeover dedupes to one durable record. |
| `completion-write-latency-signal` | `completion-records` | Write health is reported through a typed outcome taxonomy — `created`, `transient`, `terminal`, `route-missing`, `forbidden` — surfaced as degradation reasons, not through a latency measurement. `route-missing` and `forbidden` disarm the session while retaining the records; `terminal` and past-retention records are dropped. Deliberately an exception to `TELEMETRY.md`'s measure-the-async-budget rule. |
| `repository-identity-authority` | `completion-records` | Completion is keyed on `(repository, manifest.id)`, read off the resolved manifest and never off a loader URL. An explicit or resolved repository outranks any value embedded in the manifest, because the manifest schema defaults an absent repository to `interactive-tutorials` and that synthetic default would mis-key records. Only a _successful_ resolution supplies one: a failed resolution reports the last tier it probed, which is unconditionally `app-platform`, so its repository is negative-caching policy rather than an identity claim. The resolved repository travels end to end, emitter to record. A launch that carries no resolved package is not exempt: its loader synthesizes the manifest from the resource it fetched, so identity still comes off a manifest rather than off the scheme it was addressed by. |
| `completion-denominator-authority` | `completion-records` | `src/lib/guide-stats` is the only place that computes the **stamped manifest denominator** or the **percentage recorded on a durable completion record**. The per-step model in `completion-store.ts` (and the path-level rollup in the learning-paths hook) is a deliberately separate model with its own numerator and is not in scope here — see `docs/developer/STEP_MODEL.md`; migrating it is a follow-on, not a rule this row enforces today. Within scope: the module publishes the denominator AND the per-block positions together, so a numerator and a denominator can never come from two different traversals. That pairing is per guide, via `computeGuideBlockIndex`; `rollUpGuideStats` publishes a path's or journey's totals only, so a consumer recording a percentage against a rolled-up `blockCount` still needs its positions from somewhere — the rollup index lands with the first path-level consumer. Containers (`section`, `assistant`, `collapsible`) contribute their contents and nothing of their own; `multistep`, `guided`, `conditional`, and `snippet-ref` count as one block each with their children outside the denominator. `snippet-ref` makes the stamped denominator the PRE-INLINING count, so a consumer indexes the pre-inlining tree; the splice in `inline-refs.ts` carries no provenance, so mapping an inlined block back to its ref is not available. Completable means "can emit completion evidence", which is narrower than "renders interactively" — `input` only in its blocking-datasource-check shape, `grot-guide` never — and `completion-affordance.parity.test.ts` holds that in step with the runtime tracked-step registry. Progress is the furthest position with evidence of completion — a "Do it" click or a "Mark as complete" click — and is monotonic, so reaching `n` counts `1..n-1` and preamble that emits no evidence is never individually completable. There is no completable-block special case: completion is `n / n` and nothing else, so a "Do it" yields 100% only when it is the guide's final counted block. `finalCompletablePosition === blockCount` is exactly that condition, and its negation is what makes a foot-of-guide "Mark as complete" button mandatory. `percent` reads 100 only when `complete`. A change to the counting RULE must bump `GUIDE_STATS_VERSION` even though no field shape moves — that stamped version is the only thing that lets a consumer tell an old-rule stamp from a new-rule one. On the CDN path the stamp stays at `manifest.stats`; on the App Platform path both writers — `upsert-learning-path.sh` and the block editor's `deriveManifest` — put it at `spec.manifest.additionalFields.stats`, because the CRD's `#Manifest` does not declare `stats` and prunes a top-level write silently. A reader therefore has two locations to handle. The editor stamps only a plain `guide`: a path's or journey's stats are a rollup over every milestone's blocks, which a cover page cannot see, so it leaves an inherited rollup alone rather than replacing it with cover-page counts. `manifest.stats` is declared at tier 0 (`types/guide-stats.schema.ts`) and reads as absent when malformed, so `build-stats` can still repair a stale stamp. That leniency is for readers only: `validate-package` re-checks the raw value and warns that the stamp is present and ignored, so a broken stamp never looks to an author like no stamp at all. Consumers ask this module for a position; they never derive one. |
| `journey-threshold-membership` | `completion-records` | A journey completes on whole-set membership of its current milestones, never on a count. Comparing a stored completed-set's size against the milestone total lets stale slugs from a renamed or reordered path cross the threshold while current milestones are still outstanding. |

### Pre-contract candidates

These are advisory architecture hypotheses, not anchors. They cannot create a blocking conformance finding until an implementation PR establishes the owner and records the contract above. Evolution packets for these concerns set `has_recorded_anchor: false`.

<!-- prettier-ignore -->
| concern | evidence | proposed owner |
| --- | --- | --- |
| `analytics-and-telemetry` | Accreting #1275+ | A vendor-neutral typed telemetry facade with domain operations and Faro/OTel as internal adapters. Until it lands, direct vendor consumers are evidence for `contract_missing`, not an anchored-contract violation. |

---

## Footnotes

¹ `cross-cutting-architecture` related: all other concerns.

² `reversibility-and-one-way-door` reviewer output: classify each finding as `reversible`, `partially_reversible`, or `irreversible_without_cleanup`.

<!-- Authoring guidance: prefer editing existing concerns over adding new ones. Add a concern when a real missed review, recurring bug class, or important architectural blind spot appears. Tighten trigger_keywords before expanding scope. When a concern is noisy, narrow trigger_keywords or load_code, or increase min_signals. When coverage-gap detection repeatedly points at the same unowned area, first try expanding an adjacent concern before adding a new entry. Remove stale triggers that cause noisy activation. -->
